-data = data || {}

if errors
    ul.my-errors
        for error in errors
            li= error.msg
h3.form-title Welcome back! Please Login:
form(action="/login" method="POST" class="form-login")
    label Username:
    input(
        type="text"
        name="username"
        class="form-control"
    )
    label Password:
    input(
        type="password"
        name="password"
        class="form-control"
        required
    )
    input(
        type="submit" 
        value="Submit"
        class="btn btn-lg btn-primary btn-block"
        required
    )